#pragma once
#include <ImGui.h>
#include "Localization.h"

/*
class StatusBar {
public:
	// Singleton logic
	StatusBar& operator= (const StatusBar&) = delete;
	StatusBar(const StatusBar&) = delete;

	static StatusBar* getInstance() {
		if (!s_instance) s_instance = new StatusBar();
		return s_instance;
	}

	// Menu logic
	void Render();
private:
	//Singleton logic
	static StatusBar* s_instance;

	// Menu logic
	StatusBar();
};
*/